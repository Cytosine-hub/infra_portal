<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.middleware.manager.repository.ParameterStandardIndexMapper">

    <select id="findPublished" resultType="com.middleware.manager.domain.ParameterStandard">
        SELECT id, title, category, software, software_version, code, status, version,
               content, published_at, created_at, updated_at
        FROM parameter_standards
        WHERE status = 'PUBLISHED'
        ORDER BY id
    </select>
</mapper>
