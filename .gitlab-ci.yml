# ============================================================================
# 集成中心门户 · monorepo 微服务 CI/CD
#
# 设计：每个服务一条独立流水线，按【路径触发】——只有相关目录变更才构建对应服务；
#       改动共享库 common-* 或父 POM 则【扇出】重建所有服务（每个服务的 changes 里都含这几条）。
# 构建期服务之间零依赖：只用 `mvn -pl <服务> -am`（also-make，连带构建 common-* 与其能力模块）。
# 服务间真实联通（网关↔Nacos↔服务、认证越权）属【集成】层，单独一条流水线在预发环境跑。
#
# 需要：Runner Job 可访问 Runner 主机的 Docker Socket（见 runner/config.md）。
#       编译、镜像构建和 Docker Compose 部署均在同一台 Runner 主机完成，不推送 JAR 或镜像制品。
# ============================================================================

# 只在【后端、部署配置或 CI 文件】改动时创建流水线（MR / push / tag），
# 或由 GitLab Web 页面手动创建完整流水线。
# 纯文档改动（agent.md、docs、README 等）没有可跑的 job → 不创建流水线，
# 避免 GitLab 把"空流水线"判为 failed（doc-only 合并不再爆红叉）。
workflow:
  rules:
    - if: '$CI_COMMIT_TAG'
    - if: '$CI_PIPELINE_SOURCE == "web"'
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
      changes: ["backend/**/*", "deploy/**/*", "db/**/*", ".gitlab-ci.yml"]
    - if: '$CI_PIPELINE_SOURCE == "push"'
      changes: ["backend/**/*", "deploy/**/*", "db/**/*", ".gitlab-ci.yml"]

default:
  image:
    name: docker:27-cli
    pull_policy: if-not-present       # 仅本地不存在时拉取镜像
  tags: [TLZJJ]                          # 项目 Runner 标签

variables:
  DOCKER_BUILDKIT: "1"
  IMAGE_NAMESPACE: "infra-portal"

stages:
  - verify        # 编译 + 单元测试 + 打包（每服务独立）
  - integration   # 端到端 / 认证越权（需 Nacos + 多服务，手动）
  - deploy        # 部署业务服务、依赖组件或完整运行栈（手动）

# 服务构建模板：Docker 多阶段构建中执行 mvn -pl <SVC> -am clean verify，
# 然后将可执行 JAR 封装为 Runner 主机镜像。镜像只保留在 Runner 主机的 Docker 守护进程中。
.svc-verify:
  stage: verify
  script:
    - export IMAGE_NAME="$IMAGE_NAMESPACE/$SVC"
    - docker build --build-arg SERVICE="$SVC" --tag "$IMAGE_NAME:$CI_COMMIT_SHA" --tag "$IMAGE_NAME:latest" --file deploy/Dockerfile .
    - echo "== Runner 主机构建镜像：$IMAGE_NAME:$CI_COMMIT_SHA =="
    - docker image inspect "$IMAGE_NAME:$CI_COMMIT_SHA" --format 'ID={{.Id}}\n创建时间={{.Created}}\n大小={{.Size}} bytes'

# 完整业务服务构建入口。单服务构建仍复用下方 verify:<service> job，
# 可按路径自动触发，也可在 Web 流水线中逐项手动运行。
verify:all-services:
  stage: verify
  script:
    - |
      for SVC in api-gateway core-service ai-service community-service middleware-service database-service host-service network-service security-service; do
        export IMAGE_NAME="$IMAGE_NAMESPACE/$SVC"
        docker build --build-arg SERVICE="$SVC" --tag "$IMAGE_NAME:$CI_COMMIT_SHA" --tag "$IMAGE_NAME:latest" --file deploy/Dockerfile .
        docker image inspect "$IMAGE_NAME:$CI_COMMIT_SHA" --format 'ID={{.Id}}\n创建时间={{.Created}}\n大小={{.Size}} bytes'
      done
  rules:
    - if: '$CI_PIPELINE_SOURCE == "web"'
      when: manual
      allow_failure: true
    - if: '$CI_COMMIT_REF_NAME == "master"'
      when: manual
      allow_failure: true
    - changes:
        - deploy/**/*
        - db/**/*
      when: manual
      allow_failure: true

# 第三方依赖不在仓库内编译；直接拉取镜像，避免 Compose 解析业务服务密钥。
prepare:dependencies:
  stage: verify
  script:
    - docker pull "mysql:${MYSQL_VERSION:-8.0}"
    - docker pull "nacos/nacos-server:${NACOS_VERSION:-v2.3.2}"
    - docker pull "quay.io/coreos/etcd:${ETCD_VERSION:-v3.5.5}"
    - docker pull "minio/minio:${MINIO_VERSION:-RELEASE.2023-03-20T20-16-18Z}"
    - docker pull "milvusdb/milvus:${MILVUS_VERSION:-v2.3.4}"
  rules:
    - if: '$CI_PIPELINE_SOURCE == "web"'
      when: manual
      allow_failure: true
    - if: '$CI_COMMIT_REF_NAME == "master"'
      when: manual
      allow_failure: true

# ----------------------------------------------------------------------------
# 每服务流水线（路径触发）。
# 每个 changes 列表 = 该服务自身 + 其能力模块 + 【扇出段】(共享库/父POM/本文件)。
# 改共享库 → 所有服务的扇出段命中 → 全部重建。
# ----------------------------------------------------------------------------
verify:api-gateway:
  extends: .svc-verify
  variables: { SVC: api-gateway }
  rules:
    - if: '$CI_PIPELINE_SOURCE == "web"'
      when: manual
      allow_failure: true
    - changes:
        - backend/api-gateway/**/*
        - backend/pom.xml
        - backend/modular-monolith-parent/pom.xml
        - backend/common-core/**/*
        - backend/common-security/**/*
        - backend/common-web/**/*
        - .gitlab-ci.yml

verify:core-service:
  extends: .svc-verify
  variables: { SVC: core-service }
  rules:
    - if: '$CI_PIPELINE_SOURCE == "web"'
      when: manual
      allow_failure: true
    - changes:
        - backend/core-service/**/*
        - backend/identity/**/*
        - backend/catalog/**/*
        - backend/standards/**/*
        - backend/pom.xml
        - backend/modular-monolith-parent/pom.xml
        - backend/common-core/**/*
        - backend/common-security/**/*
        - backend/common-web/**/*
        - .gitlab-ci.yml

verify:ai-service:
  extends: .svc-verify
  variables: { SVC: ai-service }
  rules:
    - if: '$CI_PIPELINE_SOURCE == "web"'
      when: manual
      allow_failure: true
    - changes:
        - backend/ai-service/**/*
        - backend/knowledge/**/*
        - backend/wiki/**/*
        - backend/ops-agent/**/*
        - backend/pom.xml
        - backend/modular-monolith-parent/pom.xml
        - backend/common-core/**/*
        - backend/common-security/**/*
        - backend/common-web/**/*
        - .gitlab-ci.yml

verify:community-service:
  extends: .svc-verify
  variables: { SVC: community-service }
  rules:
    - if: '$CI_PIPELINE_SOURCE == "web"'
      when: manual
      allow_failure: true
    - changes:
        - backend/community-service/**/*
        - backend/community/**/*
        - backend/pom.xml
        - backend/modular-monolith-parent/pom.xml
        - backend/common-core/**/*
        - backend/common-security/**/*
        - backend/common-web/**/*
        - .gitlab-ci.yml

verify:middleware-service:
  extends: .svc-verify
  variables: { SVC: middleware-service }
  rules:
    - if: '$CI_PIPELINE_SOURCE == "web"'
      when: manual
      allow_failure: true
    - changes:
        - backend/middleware-service/**/*
        - backend/job-middleware/**/*
        - backend/pom.xml
        - backend/modular-monolith-parent/pom.xml
        - backend/common-core/**/*
        - backend/common-security/**/*
        - backend/common-web/**/*
        - .gitlab-ci.yml

verify:database-service:
  extends: .svc-verify
  variables: { SVC: database-service }
  rules:
    - if: '$CI_PIPELINE_SOURCE == "web"'
      when: manual
      allow_failure: true
    - changes:
        - backend/database-service/**/*
        - backend/job-database/**/*
        - backend/pom.xml
        - backend/modular-monolith-parent/pom.xml
        - backend/common-core/**/*
        - backend/common-security/**/*
        - backend/common-web/**/*
        - .gitlab-ci.yml

verify:host-service:
  extends: .svc-verify
  variables: { SVC: host-service }
  rules:
    - if: '$CI_PIPELINE_SOURCE == "web"'
      when: manual
      allow_failure: true
    - changes:
        - backend/host-service/**/*
        - backend/job-host/**/*
        - backend/pom.xml
        - backend/modular-monolith-parent/pom.xml
        - backend/common-core/**/*
        - backend/common-security/**/*
        - backend/common-web/**/*
        - .gitlab-ci.yml

verify:network-service:
  extends: .svc-verify
  variables: { SVC: network-service }
  rules:
    - if: '$CI_PIPELINE_SOURCE == "web"'
      when: manual
      allow_failure: true
    - changes:
        - backend/network-service/**/*
        - backend/job-network/**/*
        - backend/pom.xml
        - backend/modular-monolith-parent/pom.xml
        - backend/common-core/**/*
        - backend/common-security/**/*
        - backend/common-web/**/*
        - .gitlab-ci.yml

verify:security-service:
  extends: .svc-verify
  variables: { SVC: security-service }
  rules:
    - if: '$CI_PIPELINE_SOURCE == "web"'
      when: manual
      allow_failure: true
    - changes:
        - backend/security-service/**/*
        - backend/job-security/**/*
        - backend/pom.xml
        - backend/modular-monolith-parent/pom.xml
        - backend/common-core/**/*
        - backend/common-security/**/*
        - backend/common-web/**/*
        - .gitlab-ci.yml

# ----------------------------------------------------------------------------
# 集成流水线（骨架）：起 Nacos + 各服务，跑认证越权与联通验证。
# 需要 docker-in-docker 或预发环境；这里给出结构与验证清单，按你们环境补全。
# 手动触发，避免每次 push 都跑重活。
# ----------------------------------------------------------------------------
integration:e2e:
  stage: integration
  allow_failure: true          # 骨架阶段先不阻断；补全后改 false
  # services:                  # ← 取消注释并配好后启用
  #   - name: nacos/nacos-server:v2.3.2
  #     alias: nacos
  #   - name: mysql:8
  #     alias: mysql
  variables:
    GATEWAY_SIGNING_SECRET: "ci-integration-signing-secret-at-least-32-bytes"
  script:
    - echo "== 集成验证骨架（按 docs 阶段5 安全清单补全）=="
    - echo "1) 起 Nacos + MySQL + 各服务 fat jar（cloud profile，注入 GATEWAY_SIGNING_SECRET）"
    - echo "2) 经网关带有效 token 访问受保护端点 -> 期望 200"
    - echo "3) 越权A：直连下游带伪造 X-Roles 但无签名 -> 期望 401/403"
    - echo "4) 越权B：客户端经网关塞伪造 X-User -> 网关剥除后按 token 重注入"
    - echo "5) 越权C：直连 introspect 不带 X-Gateway-Sign -> 期望 403"
    - echo "6) 过期 token / 退出后同 token -> 期望 401"
    - echo "TODO 用 docker compose 起服务并用 curl 断言以上各条"
  rules:
    - if: '$CI_PIPELINE_SOURCE == "web"'
      when: manual
      allow_failure: true
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
      when: manual
      allow_failure: true
    - if: '$CI_COMMIT_REF_NAME == "master"'
      when: manual
      allow_failure: true

# ----------------------------------------------------------------------------
# 部署：使用 Runner 主机 Docker Compose 启动对应服务，镜像只读取 Runner 主机已构建版本。
# DEPLOY_ENV_FILE 必须在 GitLab CI/CD Variables 中配置为受保护的“文件”变量，
# 内容基于 deploy/services.env.example，禁止提交真实密钥。
# ----------------------------------------------------------------------------
.deploy-tpl:
  stage: deploy
  environment:
    name: "staging/$SVC"
  script:
    - test -n "${DEPLOY_ENV_FILE:-}" && test -f "$DEPLOY_ENV_FILE"
    - cp "$DEPLOY_ENV_FILE" deploy/services.env
    - trap 'rm -f deploy/services.env' EXIT
    - export IMAGE_TAG="$CI_COMMIT_SHA"
    - docker image inspect "$IMAGE_NAMESPACE/$SVC:$IMAGE_TAG" >/dev/null
    - docker compose --project-name infra-portal --env-file deploy/services.env --file deploy/docker-compose.yml up --detach --no-build --pull never "$SVC"
    - docker compose --project-name infra-portal --env-file deploy/services.env --file deploy/docker-compose.yml ps "$SVC"
  rules:
    - if: '$CI_PIPELINE_SOURCE == "web"'
      when: manual
      allow_failure: false
    - if: '$CI_COMMIT_REF_NAME == "master"'
      when: manual

# 单独部署依赖组件。MySQL 首次创建数据卷时会执行 db/init.sql 和 db/seed.sql；
# 后续部署不会重放初始化脚本。
deploy:dependencies:
  stage: deploy
  environment:
    name: staging/dependencies
  script:
    - test -n "${DEPLOY_ENV_FILE:-}" && test -f "$DEPLOY_ENV_FILE"
    - cp "$DEPLOY_ENV_FILE" deploy/services.env
    - trap 'rm -f deploy/services.env' EXIT
    - export IMAGE_TAG="$CI_COMMIT_SHA"
    - docker compose --project-name infra-portal --env-file deploy/services.env --file deploy/docker-compose.yml up --detach --no-build mysql nacos etcd minio milvus
    - docker compose --project-name infra-portal --env-file deploy/services.env --file deploy/docker-compose.yml ps mysql nacos etcd minio milvus
  rules:
    - if: '$CI_PIPELINE_SOURCE == "web"'
      when: manual
      allow_failure: false
    - if: '$CI_COMMIT_REF_NAME == "master"'
      when: manual
      allow_failure: false

# 一键构建并部署完整运行栈。该 job 独立完成构建和依赖镜像预拉取，
# 不依赖未选择的手动作业，从而可直接在 GitLab Web 流水线中执行。
deploy:full-stack:
  stage: deploy
  environment:
    name: staging/full-stack
  script:
    - test -n "${DEPLOY_ENV_FILE:-}" && test -f "$DEPLOY_ENV_FILE"
    - cp "$DEPLOY_ENV_FILE" deploy/services.env
    - trap 'rm -f deploy/services.env' EXIT
    - export IMAGE_TAG="$CI_COMMIT_SHA"
    - |
      for SVC in api-gateway core-service ai-service community-service middleware-service database-service host-service network-service security-service; do
        docker build --build-arg SERVICE="$SVC" --tag "$IMAGE_NAMESPACE/$SVC:$IMAGE_TAG" --tag "$IMAGE_NAMESPACE/$SVC:latest" --file deploy/Dockerfile .
      done
    - docker compose --project-name infra-portal --env-file deploy/services.env --file deploy/docker-compose.yml pull mysql nacos etcd minio milvus
    - docker compose --project-name infra-portal --env-file deploy/services.env --file deploy/docker-compose.yml up --detach --no-build --pull never
    - docker compose --project-name infra-portal --env-file deploy/services.env --file deploy/docker-compose.yml ps
  rules:
    - if: '$CI_PIPELINE_SOURCE == "web"'
      when: manual
      allow_failure: false
    - if: '$CI_COMMIT_REF_NAME == "master"'
      when: manual
      allow_failure: false

deploy:api-gateway:
  extends: .deploy-tpl
  variables: { SVC: api-gateway }
  needs: [{ job: "verify:api-gateway", optional: true }]

deploy:core-service:
  extends: .deploy-tpl
  variables: { SVC: core-service }
  needs: [{ job: "verify:core-service", optional: true }]

deploy:ai-service:
  extends: .deploy-tpl
  variables: { SVC: ai-service }
  needs: [{ job: "verify:ai-service", optional: true }]

deploy:community-service:
  extends: .deploy-tpl
  variables: { SVC: community-service }
  needs: [{ job: "verify:community-service", optional: true }]

deploy:middleware-service:
  extends: .deploy-tpl
  variables: { SVC: middleware-service }
  needs: [{ job: "verify:middleware-service", optional: true }]

deploy:database-service:
  extends: .deploy-tpl
  variables: { SVC: database-service }
  needs: [{ job: "verify:database-service", optional: true }]

deploy:host-service:
  extends: .deploy-tpl
  variables: { SVC: host-service }
  needs: [{ job: "verify:host-service", optional: true }]

deploy:network-service:
  extends: .deploy-tpl
  variables: { SVC: network-service }
  needs: [{ job: "verify:network-service", optional: true }]

deploy:security-service:
  extends: .deploy-tpl
  variables: { SVC: security-service }
  needs: [{ job: "verify:security-service", optional: true }]
